package com.example.automation

enum class RepeatType {
    ONCE,          // Trigger only the first time ever
    EVERY_TIME,    // Trigger every time user enters
    DAILY          // Trigger once per day on enter
}
